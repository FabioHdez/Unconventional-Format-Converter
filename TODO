FLASK:
    SEND FILES
    DELETE FOLDERS

REACT:
    UPDATE OUTPUT CONVERSION COLUMN AFTER RECEIVING THE RESPONSE FROM FLASK SERVER
    DOWNLOAD BUTTON TO BE RENDERED ONLY AFTER RECEIVING THE RESPONSE FROM FLASK SERVER